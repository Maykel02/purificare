---
const { href, title, body } = Astro.props;
---

<div class="flex flex-col items-center lg:gap-5 gap-3 p-3">
    <img class="size-40" src={href} alt="">
    <div class="text-center">
        <h3 class="text-xl font-semibold text-[--color-primary]">{title}</h3>
        <p class="text-md">{body}</p>
    </div>
</div>